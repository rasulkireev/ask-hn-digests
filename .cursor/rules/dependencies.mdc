---
alwaysApply: true
---

Do not edit dependency files directly.
Always add dependencies with `uv add ...` (or `uv add --dev ...` for dev dependencies), then update the lockfile with `uv lock` when needed.
